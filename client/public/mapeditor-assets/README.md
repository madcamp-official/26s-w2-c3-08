여기에 파일만 넣으면 코드 수정 없이 맵 에디터에 바로 반영됩니다 (client/src/mapeditor/render/images.ts).

| 파일명 | 용도 | 권장 형태 |
|---|---|---|
| flag-start.png | 시작 깃발 | 투명 배경, 세로가 긴 비율(깃대+깃발) |
| flag-end.png | 끝(체크포인트) 깃발 | 위와 동일 |
| flag-base.png | 깃발 아래 3×1 기단 | 투명 배경, 가로가 긴 비율(3:1) |
| cursor.png | 배치 모드 커스텀 마우스 커서 | 작은 정사각형, 32×32 권장 |

없으면 자동으로 기존 벡터 드로잉(색깔 도형)으로 폴백됩니다 — 파일을 넣거나 빼는 것만으로 즉시 전환됩니다(새로고침 필요).
