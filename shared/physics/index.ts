// 클라(예측)·서버(권위 판정)가 공유하는 물리 순수 함수 모듈.
// 스파이크(archive/spikes/phaser-ramp) 검증 로직을 이식하되,
// game feel 요소(가속/감속 곡선, 코요테 타임, 점프 버퍼, 가변 점프)를
// 보강한 2차 물리 작업으로 채운다. — docs/KJH/player-spec.md 참조
//
// 원칙: Phaser·DOM 등 환경 의존 금지 (Node/브라우저 양쪽에서 동일 실행)

export {};
