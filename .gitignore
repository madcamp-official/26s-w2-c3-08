# 세션 작업물 보관 — docs/plans/spikes는 추적(핸드오프 문서 등), reference/는 계속 로컬 전용
# (참고용으로 클론해온 남의 저장소 — 중첩 .git 포함 + 66MB, 커밋하면 저장소가 영구히 불어남)
archive/reference/

# 공통
node_modules/
.env
.env.*
dist/

# 비밀 정보 (접속 정보·키) — 절대 커밋 금지
*.local.md
secrets.local.*
