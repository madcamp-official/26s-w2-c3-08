{
  "name": "26s-w2-c3-08",
  "private": true,
  "scripts": {
    "dev:v2": "node scripts/dev-v2-services.mjs",
    "dev:backend": "npm run dev --prefix backend",
    "dev:client": "npm run dev --workspace client",
    "check:production-env": "node scripts/check-production-env.mjs",
    "check:production-env:self-test": "node scripts/check-production-env.mjs --self-test",
    "check:v2:client": "npm run tokens:check --workspace client && npm run primitives:check --workspace client && npm run core:check --workspace client && npm run shells:check --workspace client && npm run studio:check --workspace client && npm run login:check --workspace client && npm run main:check --workspace client && npm run warehouse:check --workspace client && npm run avatar-studio:check --workspace client && npm run asset-studio:check --workspace client && npm run lobby-room:check --workspace client && npm run game:check --workspace client && npm run realtime:check --workspace client && npm run launcher:check --workspace client && npm run flow:check --workspace client",
    "check:v2": "npm run check:production-env:self-test && npm run lint --workspace client -- --quiet && npm run smoke --workspace client && npm run check:v2:client && npm run build --workspace client && npm run test --prefix backend && npm run typecheck --prefix backend && npm run build --prefix backend && npm run test --prefix server && npm run check --prefix gpu-worker && git diff --check"
  },
  "workspaces": [
    "backend",
    "client",
    "server",
    "shared",
    "gpu-worker"
  ]
}
